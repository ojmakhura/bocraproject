// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by SpringWSImpl.java.vsl in andromda-webservices.
//
package bw.org.bocra.portal.form.kpi;

import bw.org.bocra.portal.form.FormCriteria;
import java.util.Collection;
import java.util.Optional;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("kpi")
@CrossOrigin()
public class KpiRestControllerImpl extends KpiRestControllerBase {

    protected static Logger log = LoggerFactory.getLogger(KpiRestControllerImpl.class);


    @Override
    public ResponseEntity<KpiVO> handleFindById(Long id) {
        Optional<KpiVO> data = Optional.empty(); // TODO: Add custom code here;
        ResponseEntity<KpiVO> response;

        if(data.isPresent()) {
            response = ResponseEntity.status(HttpStatus.OK).body(data.get());
        } else {
            response = ResponseEntity.status(HttpStatus.NOT_FOUND).build();
        }

        return response;
    }

    @Override
    public ResponseEntity<Collection<KpiVO>> handleGetAll() {
        Optional<Collection<KpiVO>> data = Optional.empty(); // TODO: Add custom code here;
        ResponseEntity<Collection<KpiVO>> response;

        if(data.isPresent()) {
            response = ResponseEntity.status(HttpStatus.OK).body(data.get());
        } else {
            response = ResponseEntity.status(HttpStatus.NOT_FOUND).build();
        }

        return response;
    }

    @Override
    public ResponseEntity<Boolean> handleRemove(Long id) {
        Optional<Boolean> data = Optional.empty(); // TODO: Add custom code here;
        ResponseEntity<Boolean> response;

        if(data.isPresent()) {
            response = ResponseEntity.status(HttpStatus.OK).body(data.get());
        } else {
            response = ResponseEntity.status(HttpStatus.NOT_FOUND).build();
        }

        return response;
    }

    @Override
    public ResponseEntity<KpiVO> handleSave(KpiVO kpiVO) {
        Optional<KpiVO> data = Optional.empty(); // TODO: Add custom code here;
        ResponseEntity<KpiVO> response;

        if(data.isPresent()) {
            response = ResponseEntity.status(HttpStatus.OK).body(data.get());
        } else {
            response = ResponseEntity.status(HttpStatus.NOT_FOUND).build();
        }

        return response;
    }

    @Override
    public ResponseEntity<Collection<KpiVO>> handleSearch(FormCriteria searchCriteria) {
        Optional<Collection<KpiVO>> data = Optional.empty(); // TODO: Add custom code here;
        ResponseEntity<Collection<KpiVO>> response;

        if(data.isPresent()) {
            response = ResponseEntity.status(HttpStatus.OK).body(data.get());
        } else {
            response = ResponseEntity.status(HttpStatus.NOT_FOUND).build();
        }

        return response;
    }
}