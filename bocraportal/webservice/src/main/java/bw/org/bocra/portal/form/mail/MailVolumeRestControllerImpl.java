// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by SpringWSImpl.java.vsl in andromda-webservices.
//
package bw.org.bocra.portal.form.mail;

import java.util.Collection;
import java.util.Optional;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("mail")
@CrossOrigin()
public class MailVolumeRestControllerImpl extends MailVolumeRestControllerBase {

    protected static Logger log = LoggerFactory.getLogger(MailVolumeRestControllerImpl.class);


    @Override
    public ResponseEntity<MailVolumeVO> handleFindById(Long id) {
        Optional<MailVolumeVO> data = Optional.of(mailVolumeService.findById(id));
        ResponseEntity<MailVolumeVO> response;

        if(data.isPresent()) {
            response = ResponseEntity.status(HttpStatus.OK).body(data.get());
        } else {
            response = ResponseEntity.status(HttpStatus.NOT_FOUND).build();
        }

        return response;
    }

    @Override
    public ResponseEntity<Collection<MailVolumeVO>> handleGetAll() {
        Optional<Collection<MailVolumeVO>> data = Optional.of(mailVolumeService.getAll());
        ResponseEntity<Collection<MailVolumeVO>> response;

        if(data.isPresent()) {
            response = ResponseEntity.status(HttpStatus.OK).body(data.get());
        } else {
            response = ResponseEntity.status(HttpStatus.NOT_FOUND).build();
        }

        return response;
    }

    @Override
    public ResponseEntity<Boolean> handleRemove(Long id) {
        Optional<Boolean> data = Optional.of(mailVolumeService.remove(id));
        ResponseEntity<Boolean> response;

        if(data.isPresent()) {
            response = ResponseEntity.status(HttpStatus.OK).body(data.get());
        } else {
            response = ResponseEntity.status(HttpStatus.NOT_FOUND).build();
        }

        return response;
    }

    @Override
    public ResponseEntity<MailVolumeVO> handleSave(MailVolumeVO mailVolumeVO) {
        Optional<MailVolumeVO> data = Optional.of(mailVolumeService.save(mailVolumeVO));
        ResponseEntity<MailVolumeVO> response;

        if(data.isPresent()) {
            response = ResponseEntity.status(HttpStatus.OK).body(data.get());
        } else {
            response = ResponseEntity.status(HttpStatus.NOT_FOUND).build();
        }

        return response;
    }

    @Override
    public ResponseEntity<Collection<MailVolumeVO>> handleSearch(MailVolumeCriteria searchCriteria) {
        Optional<Collection<MailVolumeVO>> data = Optional.of(mailVolumeService.search(searchCriteria));
        ResponseEntity<Collection<MailVolumeVO>> response;

        if(data.isPresent()) {
            response = ResponseEntity.status(HttpStatus.OK).body(data.get());
        } else {
            response = ResponseEntity.status(HttpStatus.NOT_FOUND).build();
        }

        return response;
    }
}