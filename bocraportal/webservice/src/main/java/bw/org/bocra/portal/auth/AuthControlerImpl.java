// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by SpringWSImpl.java.vsl in andromda-webservices.
//
package bw.org.bocra.portal.auth;

import java.util.Optional;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/user")
@CrossOrigin()
public class AuthControlerImpl extends AuthControlerBase {

    protected static Logger log = LoggerFactory.getLogger(AuthControlerImpl.class);


    @Override
    public ResponseEntity<UserVO> handleCreateUser(UserVO userVO) {
        Optional<UserVO> data = Optional.empty(); // TODO: Add custom code here;
        ResponseEntity<UserVO> response;

        if(data.isPresent()) {
            response = ResponseEntity.status(HttpStatus.OK).body(data.get());
        } else {
            response = ResponseEntity.status(HttpStatus.NOT_FOUND).build();
        }

        return response;
    }

    @Override
    public ResponseEntity<String> handleGetEmail() {
        Optional<String> data = Optional.empty(); // TODO: Add custom code here;
        ResponseEntity<String> response;

        if(data.isPresent()) {
            response = ResponseEntity.status(HttpStatus.OK).body(data.get());
        } else {
            response = ResponseEntity.status(HttpStatus.NOT_FOUND).build();
        }

        return response;
    }

    @Override
    public ResponseEntity<String> handleGetName() {
        Optional<String> data = Optional.empty(); // TODO: Add custom code here;
        ResponseEntity<String> response;

        if(data.isPresent()) {
            response = ResponseEntity.status(HttpStatus.OK).body(data.get());
        } else {
            response = ResponseEntity.status(HttpStatus.NOT_FOUND).build();
        }

        return response;
    }

    @Override
    public ResponseEntity<Object> handleSignin(UserVO userVO) {
        Optional<Object> data = Optional.empty(); // TODO: Add custom code here;
        ResponseEntity<Object> response;

        if(data.isPresent()) {
            response = ResponseEntity.status(HttpStatus.OK).body(data.get());
        } else {
            response = ResponseEntity.status(HttpStatus.NOT_FOUND).build();
        }

        return response;
    }
}