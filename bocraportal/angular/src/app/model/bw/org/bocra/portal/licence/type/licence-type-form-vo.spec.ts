import { LicenceTypeFormVO } from './licence-type-form-vo';

describe('LicenceTypeFormVO', () => {
	it('it should create an instance', () => {
		expect(new LicenceTypeFormVO()).toBeTruthy();
	});
});

