import { FormActivationCriteria } from './form-activation-criteria';

describe('FormActivationCriteria', () => {
	it('it should create an instance', () => {
		expect(new FormActivationCriteria()).toBeTruthy();
	});
});

