// Generated by andromda-angular cartridge (app\usecase\selector.store.ts.vsl) DO NOT EDIT
import { createFeatureSelector, createSelector } from '@ngrx/store';
import { periodConfigKey, PeriodConfigState } from './periodConfig.state';

export const selectPeriodConfigState = createFeatureSelector<PeriodConfigState> (
    periodConfigKey
);
export const selectPeriodConfigVO = createSelector(
    selectPeriodConfigState,
    (state: PeriodConfigState) => state.periodConfigVO
);

export const selectSearchCriteria = createSelector(
    selectPeriodConfigState,
    (state: PeriodConfigState) => state.searchCriteria
);

export const selectPageNumber = createSelector(
    selectPeriodConfigState,
    (state: PeriodConfigState) => state.pageNumber
);

export const selectPageSize = createSelector(
    selectPeriodConfigState,
    (state: PeriodConfigState) => state.pageSize
);

export const selectId = createSelector(
    selectPeriodConfigState,
    (state: PeriodConfigState) => state.id
);

export const selectError = createSelector(
    selectPeriodConfigState,
    (state: PeriodConfigState) => state.error
);

