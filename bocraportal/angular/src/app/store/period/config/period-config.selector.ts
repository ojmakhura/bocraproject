// Generated by andromda-angular cartridge (app\usecase\action.store.ts.vsl) DO NOT EDIT
import { createFeatureSelector, createSelector } from '@ngrx/store';
import { periodConfigKey, PeriodConfigState } from './period-config.reducer';

export const selectPeriodConfigState = createFeatureSelector<PeriodConfigState>(periodConfigKey);

export const selectPeriodConfig = createSelector(
    selectPeriodConfigState,
      (state: PeriodConfigState) => state.periodConfig
);

export const selectGroups = createSelector(
    selectPeriodConfigState,
      (state: PeriodConfigState) => state.periodConfigs
);

export const selectError = createSelector(
    selectPeriodConfigState,
      (state: PeriodConfigState) => state.error
);

export const selectId = createSelector(
    selectPeriodConfigState,
      (state: PeriodConfigState) => state.id
);

export const selectLoading = createSelector(
    selectPeriodConfigState,
      (state: PeriodConfigState) => state.loading
);

export const selectCriteria = createSelector(
    selectPeriodConfigState,
      (state: PeriodConfigState) => state.searchCriteria
);
