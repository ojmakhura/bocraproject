// Generated by andromda-angular cartridge (app\usecase\action.store.ts.vsl) DO NOT EDIT
import { createFeatureSelector, createSelector } from '@ngrx/store';
import { smsTrafficKey, SMSTrafficState } from './sms-traffic.reducer';

export const selectSMSTrafficState = createFeatureSelector<SMSTrafficState>(smsTrafficKey);

export const selectSMSTraffic = createSelector(
    selectSMSTrafficState,
      (state: SMSTrafficState) => state.smsTraffic
);

export const selectSMSTraffics = createSelector(
    selectSMSTrafficState,
      (state: SMSTrafficState) => state.smsTraffics
);

export const selectError = createSelector(
    selectSMSTrafficState,
      (state: SMSTrafficState) => state.error
);

export const selectId = createSelector(
    selectSMSTrafficState,
      (state: SMSTrafficState) => state.id
);

export const selectLoading = createSelector(
    selectSMSTrafficState,
      (state: SMSTrafficState) => state.loading
);

export const selectCriteria = createSelector(
    selectSMSTrafficState,
      (state: SMSTrafficState) => state.searchCriteria
);
