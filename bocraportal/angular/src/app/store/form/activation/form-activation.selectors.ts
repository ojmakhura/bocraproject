// Generated by andromda-angular cartridge (app\usecase\selector.store.ts.vsl) DO NOT EDIT
import { createFeatureSelector, createSelector } from '@ngrx/store';
import { formActivationKey, FormActivationState } from './form-activation.state';

export const selectFormActivationState = createFeatureSelector<FormActivationState> (
    formActivationKey
);

export const selectFormActivation = createSelector(
    selectFormActivationState,
    (state: FormActivationState) => state.formActivation
);

export const selectFormActivations = createSelector(
    selectFormActivationState,
    (state: FormActivationState) => state.formActivations
);

export const selectFormActivationsPage = createSelector(
    selectFormActivationState,
    (state: FormActivationState) => state.formActivationsPage
);

export const selectActivationName = createSelector(
    selectFormActivationState,
    (state: FormActivationState) => state.activationName
);

export const selectId = createSelector(
    selectFormActivationState,
    (state: FormActivationState) => state.id
);

export const selectRemoved = createSelector(
    selectFormActivationState,
    (state: FormActivationState) => state.removed
);

export const selectLoaderMessage = createSelector(
    selectFormActivationState,
    (state: FormActivationState) => state.loaderMessage
);

export const selectMessages = createSelector(
    selectFormActivationState,
    (state: FormActivationState) => state.messages
);

export const selectSuccess = createSelector(
    selectFormActivationState,
    (state: FormActivationState) => state.success
);

export const selectLoading = createSelector(
    selectFormActivationState,
    (state: FormActivationState) => state.loading
);

export const selectError = createSelector(
    selectFormActivationState,
    (state: FormActivationState) => state.error
);
