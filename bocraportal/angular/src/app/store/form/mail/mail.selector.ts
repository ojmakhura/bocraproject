// Generated by andromda-angular cartridge (app\usecase\action.store.ts.vsl) DO NOT EDIT
import { createFeatureSelector, createSelector } from '@ngrx/store';
import { mailKey, MailState } from './mail.reducer';

export const selectMailState = createFeatureSelector<MailState>(mailKey);

export const selectMail = createSelector(
    selectMailState,
      (state: MailState) => state.mail
);

export const selectMails = createSelector(
    selectMailState,
      (state: MailState) => state.mails
);

export const selectError = createSelector(
    selectMailState,
      (state: MailState) => state.error
);

export const selectId = createSelector(
    selectMailState,
      (state: MailState) => state.id
);

export const selectLoading = createSelector(
    selectMailState,
      (state: MailState) => state.loading
);

export const selectCriteria = createSelector(
    selectMailState,
      (state: MailState) => state.searchCriteria
);
