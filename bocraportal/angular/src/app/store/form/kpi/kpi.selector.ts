// Generated by andromda-angular cartridge (app\usecase\action.store.ts.vsl) DO NOT EDIT
import { createFeatureSelector, createSelector } from '@ngrx/store';
import { kpiKey, KpiState } from './kpi.reducer';

export const selectKpiState = createFeatureSelector<KpiState>(kpiKey);

export const selectKpi = createSelector(
    selectKpiState,
      (state: KpiState) => state.kpi
);

export const selectKpis = createSelector(
    selectKpiState,
      (state: KpiState) => state.kpis
);

export const selectError = createSelector(
    selectKpiState,
      (state: KpiState) => state.error
);

export const selectId = createSelector(
    selectKpiState,
      (state: KpiState) => state.id
);

export const selectLoading = createSelector(
    selectKpiState,
      (state: KpiState) => state.loading
);

export const selectCriteria = createSelector(
    selectKpiState,
      (state: KpiState) => state.searchCriteria
);
