// Generated by andromda-angular cartridge (app\usecase\action.store.ts.vsl) DO NOT EDIT
import { createFeatureSelector, createSelector } from '@ngrx/store';
import { simKey, SimState } from './sim.reducer';

export const selectSimState = createFeatureSelector<SimState>(simKey);

export const selectSim = createSelector(
    selectSimState,
      (state: SimState) => state.sim
);

export const selectSims = createSelector(
    selectSimState,
      (state: SimState) => state.sims
);

export const selectError = createSelector(
    selectSimState,
      (state: SimState) => state.error
);

export const selectId = createSelector(
    selectSimState,
      (state: SimState) => state.id
);

export const selectLoading = createSelector(
    selectSimState,
      (state: SimState) => state.loading
);

export const selectCriteria = createSelector(
    selectSimState,
      (state: SimState) => state.searchCriteria
);
