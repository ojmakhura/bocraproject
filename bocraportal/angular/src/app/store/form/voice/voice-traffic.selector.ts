// Generated by andromda-angular cartridge (app\usecase\action.store.ts.vsl) DO NOT EDIT
import { createFeatureSelector, createSelector } from '@ngrx/store';
import { voiceTrafficKey, VoiceTrafficState } from './voice-traffic.reducer';

export const selectVoiceTrafficState = createFeatureSelector<VoiceTrafficState>(voiceTrafficKey);

export const selectVoiceTraffic = createSelector(
    selectVoiceTrafficState,
      (state: VoiceTrafficState) => state.voiceTraffic
);

export const selectVoiceTraffics = createSelector(
    selectVoiceTrafficState,
      (state: VoiceTrafficState) => state.voiceTraffics
);

export const selectError = createSelector(
    selectVoiceTrafficState,
      (state: VoiceTrafficState) => state.error
);

export const selectId = createSelector(
    selectVoiceTrafficState,
      (state: VoiceTrafficState) => state.id
);

export const selectLoading = createSelector(
    selectVoiceTrafficState,
      (state: VoiceTrafficState) => state.loading
);

export const selectCriteria = createSelector(
    selectVoiceTrafficState,
      (state: VoiceTrafficState) => state.searchCriteria
);
