// Generated by andromda-angular cartridge (app\usecase\selector.store.ts.vsl) DO NOT EDIT
import { createFeatureSelector, createSelector } from '@ngrx/store';
import { guardKey, GuardState } from './guard.state';

export const selectGuardState = createFeatureSelector<GuardState> (
    guardKey
);
export const selectUrlGuardVO = createSelector(
    selectGuardState,
    (state: GuardState) => state.urlGuardVO
);

export const selectId = createSelector(
    selectGuardState,
    (state: GuardState) => state.id
);

export const selectCriteria = createSelector(
    selectGuardState,
    (state: GuardState) => state.criteria
);

export const selectPageNumber = createSelector(
    selectGuardState,
    (state: GuardState) => state.pageNumber
);

export const selectPageSize = createSelector(
    selectGuardState,
    (state: GuardState) => state.pageSize
);

export const selectError = createSelector(
    selectGuardState,
    (state: GuardState) => state.error
);

