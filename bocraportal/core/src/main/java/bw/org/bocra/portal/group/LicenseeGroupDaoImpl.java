// Generated by hibernate/SpringHibernateDaoImpl.vsl in andromda-spring-cartridge on 02/11/2022 11:23:25+0200.
// license-header java merge-point
/**
 * This is only generated once! It will never be overwritten.
 * You can (and have to!) safely modify it by hand.
 */
package bw.org.bocra.portal.group;

import java.util.ArrayList;
import java.util.Collection;

import org.apache.commons.lang3.StringUtils;
import org.springframework.data.jpa.domain.Specification;
import org.springframework.stereotype.Repository;

import bw.org.bocra.portal.licensee.Licensee;
import bw.org.bocra.portal.licensee.LicenseeVO;

/**
 * @see LicenseeGroup
 */
@Repository("licenseeGroupDao")
public class LicenseeGroupDaoImpl
    extends LicenseeGroupDaoBase
{
    /**
     * {@inheritDoc}
     */
    @Override
    public void toLicenseeGroupVO(
        LicenseeGroup source,
        LicenseeGroupVO target)
    {
        // TODO verify behavior of toLicenseeGroupVO
        super.toLicenseeGroupVO(source, target);
        // WARNING! No conversion for target.licensees (can't convert source.getLicensees():bw.org.bocra.portal.licensee.Licensee to bw.org.bocra.portal.licensee.LicenseeVO
        if(source.getLicensees() != null) {
            ArrayList<LicenseeVO> licensees = new ArrayList<>();
            for (Licensee licensee : source.getLicensees()) {
                LicenseeVO lvo = new LicenseeVO();
                lvo.setId(licensee.getId());
                lvo.setAddress(licensee.getAddress());
                lvo.setLicenseeName(licensee.getLicenseeName());
                lvo.setUin(licensee.getUin());
            }

            target.setLicensees(licensees);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public LicenseeGroupVO toLicenseeGroupVO(final LicenseeGroup entity)
    {
        // TODO verify behavior of toLicenseeGroupVO
        return super.toLicenseeGroupVO(entity);
    }

    /**
     * Retrieves the entity object that is associated with the specified value object
     * from the object store. If no such entity object exists in the object store,
     * a new, blank entity is created
     */
    private LicenseeGroup loadLicenseeGroupFromLicenseeGroupVO(LicenseeGroupVO licenseeGroupVO)
    {
        if (licenseeGroupVO.getId() == null)
        {
            return  LicenseeGroup.Factory.newInstance();
        }
        else
        {
            return this.load(licenseeGroupVO.getId());
        }
    }

    /**
     * {@inheritDoc}
     */
    public LicenseeGroup licenseeGroupVOToEntity(LicenseeGroupVO licenseeGroupVO)
    {
        // TODO verify behavior of licenseeGroupVOToEntity
        LicenseeGroup entity = this.loadLicenseeGroupFromLicenseeGroupVO(licenseeGroupVO);
        this.licenseeGroupVOToEntity(licenseeGroupVO, entity, true);
        return entity;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void licenseeGroupVOToEntity(
        LicenseeGroupVO source,
        LicenseeGroup target,
        boolean copyIfNull)
    {
        // TODO verify behavior of licenseeGroupVOToEntity
        super.licenseeGroupVOToEntity(source, target, copyIfNull);
        if(source.getLicensees() != null) {
            Collection<Licensee> licensees = new ArrayList<>();
            for (LicenseeVO licensee : source.getLicensees()) {
                Licensee lvo = Licensee.Factory.newInstance();
                lvo.setId(licensee.getId());
                lvo.setAddress(licensee.getAddress());
                lvo.setLicenseeName(licensee.getLicenseeName());
                lvo.setUin(licensee.getUin());
            }

            target.setLicensees(licensees);
        }
    }

    @Override
    protected Collection<LicenseeGroup> handleFindByCriteria(LicenseeGroupCriteria criteria) throws Exception {
        // TODO Auto-generated method stub

        Specification<LicenseeGroup> criteriaSpecs =  null;

        if(StringUtils.isNotBlank(criteria.getGroupSearch())) {

            criteriaSpecs = LicenseeGroupSpecifications.findByCodeLikeIgnoreCase(criteria.getGroupSearch());
            criteriaSpecs.and(LicenseeGroupSpecifications.findByGroupNameLikeIgnoreCase(criteria.getGroupSearch()));
            criteriaSpecs.and(LicenseeGroupSpecifications.findByDescriptionLikeIgnoreCase(criteria.getGroupSearch()));
        }

        if(criteria.getLicenseeId() != null) {
            if(criteriaSpecs == null) {

            } else {

            }
        }

        return criteriaSpecs == null ? licenseeGroupRepository.findAll() : licenseeGroupRepository.findAll(criteriaSpecs);
    }
}